python垃圾回收机制使用的主要算法是引用计数。实际上，每个对象都会统计有多少个引用指向自己。当引用计数归零时，对象立即就被销毁：CPython会在对象上调用__del__方法（如果定义了），然后释放分配给对象的内存。
CPython 2.0增加了分代垃圾回收算法，用于检测循环过程中涉及的对象组——如果一组对象之间全是互相引用，即使再出色的引用方式也会导致组中对象不可获取。
弱引用并不会妨碍所指对象被当作垃圾回收。
